name: La mera verdura

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  greeting:

    runs-on: ubuntu-latest

    steps:
    - name: Print greeting
      run: echo "Hello, world"
