name: La mera verdura webhook

on:
    repository_dispatch:
        types: [run_mada_test, deploy_to_madafaking]

jobs:
    run:
        runs-on: ubuntu-latest
        steps:
            - name: Doble echo
              run: | 
                echo "Event type: ${{ github.event.action }}"
                echo "Payload value ${{ github.event.client_payload.value }}"